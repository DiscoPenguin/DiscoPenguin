import { AppBusyDirective } from './app-busy.directive';

describe('AppBusyDirective', () => {
  it('should create an instance', () => {
    const directive = new AppBusyDirective();
    expect(directive).toBeTruthy();
  });
});
